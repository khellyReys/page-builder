WhataHotel Netlify Agent Rules (edit ONLY `src/data/promo-N.ts` + `src/data/promos.ts`).
Never edit `src/types.ts` or `src/components/`. If types mismatch docs, stop and report branch needs sync.

Read `docs/whatahotel-agent-new-promo.md` first. Do not read full SSOT in one call.
Never use old `promo-*.ts` as template. Use `src/types.ts` + docs + booking HTML only.

Fetch each booking URL ONCE. Parse full HTML (not summaries). If fetch tool summarizes/refuses, use curl once and parse markers (`subSlides`, `booking-img-list`, `booking_info.cfm`).

Images:
- Hero: `ul#subSlides` first `li` background-image URL -> prepend `https://whatahotel.com` -> set BOTH `hero.imageUrl` + `thumbnailUrl`.
- Rooms: `.bookingItem ul.booking-img-list li a[href]` only (NOT `img src`). First 2 valid links per room. Valid CDNs: `d2573qu6qrjt8c` or `d321ocj5nbe62c`.
- Skip placeholders (`/img/paceholder.jpg`). If room has only placeholder/logo, use `images: []` (do not invent/fallback room image).
- User city image URL goes ONLY to `hero.cityImageUrl` + `hero.cityImageAlt`.

Rates:
- ADR is EXCL. taxes/fees.
- Grand Total is INCL. taxes/fees whenever provided by booking page.
- UI copy rule: do NOT add "excluding taxes/fees" text next to ADR; only Grand Total should carry the "incl. taxes & fees" note.
- Canonical equality per room: `grandTotalInclTaxes = bookingSummary.total = savings.rightValue`.
- `portalTotalValue` = lowest featured room grand total (across all featured rooms/all hotels).
- Use lowest offer row for `bookUrl`; prepend `https://www.whatahotel.com` to relative links.
- Do NOT compute/show Standard-vs-Offered or “You Save” comparisons; keep `priceStrike: ""`.

Mode:
- 1 hotelID => single-hotel (`hero/offer/rooms`)
- 2+ distinct hotelIDs => `hotels[]` with strict per-URL data isolation.

Per room required: nights, ADR, grand total, bookingSummary, features (`door-open` and `gift`), ISO `createdAt`.
Default when unspecified: 3 lowest-priced rooms, next free promo id from `promos.ts`, client = `""`.
Density rule (avoid large vertical gaps between room cards):
- Keep copy concise (no long repeated paragraphs).
- Do not add empty placeholders / filler text / extra `<br>` spacing.
- If a section has no real data (images, notes, etc.), leave the field empty and let UI skip it.

Hard gate: do NOT output completion until all checks pass and `npm run build` passes.
If any check fails, report field-level failures and fix before final output.

Final output must include:
Validation Report (PASS/FAIL with counts X/Y):
1) Rooms parsed
2) Nights present
3) ADR present
4) Grand total present
5) Equality check (`grandTotalInclTaxes=bookingSummary.total=savings.rightValue`)
6) Logical check (for multi-night stays, grand total >= ADR)
7) `portalTotalValue` matches lowest featured room total
8) Hero/room image extraction rule compliance
9) `npm run build` status
Only print `PROMO COMPLETE` if all are PASS.
