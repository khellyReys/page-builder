[build]
  publish = "dist"
  command = "npm run build"
  functions = "netlify/functions"

# Static files (e.g. dist/promo/promo-N/index.html) are served before this fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
