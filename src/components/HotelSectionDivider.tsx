/** Full-width burgundy→border line + cream strip, matching `CityHeroImage` header (without label). */
export function HotelSectionDivider() {
  return (
    <div className="hotel-section-divider" aria-hidden="true">
      <span className="hotel-section-divider-line" />
    </div>
  );
}
